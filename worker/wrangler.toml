name = "sprintpad-sync"
main = "index.js"
compatibility_date = "2026-01-01"

# Create the namespace once, then paste its id here:
#   npx wrangler kv namespace create PADS
[[kv_namespaces]]
binding = "PADS"
id = "REPLACE_WITH_YOUR_KV_NAMESPACE_ID"
