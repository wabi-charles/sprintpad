name = "sprintpad-sync"
main = "index.js"
compatibility_date = "2026-01-01"

# A namespace id, not a secret: it is useless without access to the Cloudflare
# account that owns it. Deploying your own means replacing it with your own,
# created by: npx wrangler kv namespace create PADS
[[kv_namespaces]]
binding = "PADS"
id = "9a6226c810ed4b0ebdc822dfb23b5bda"
